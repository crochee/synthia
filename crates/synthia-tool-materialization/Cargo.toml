[package]
name = "synthia-tool-materialization"
version.workspace = true
edition.workspace = true
description = "Tool identity, provenance, and materialization types for Synthia"

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
uuid.workspace = true

[features]
default = []

[lints.rust]
unsafe_code = "forbid"
